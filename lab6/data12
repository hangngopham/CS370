
/* a complicated example that involves conditional expressions */
 
int test6(int x, int y)
{
	int z;
	int q;
	
	z = (x == 0 ? y : 0);
	
	q = (x == 0 ? y == 0 ? 1 : 2 : z == 0 ? 3 : 4);
	
	return z + q;
}

